```markdown
# 🌳 ДПК «Заповедное» & «Колосок» — объединённый сайт дачных посёлков

> **Полное руководство по установке, запуску, использованию и разработке**

---

## 📖 Оглавление

1. [О проекте](#-о-проекте)
2. [Технологический стек](#-технологический-стек)
3. [Требования для запуска](#-требования-для-запуска)
4. [Установка и запуск (локально)](#-установка-и-запуск-локально)
   - [4.1 Клонирование репозитория](#41-клонирование-репозитория)
   - [4.2 Запуск бэкенда](#42-запуск-бэкенда)
   - [4.3 Запуск фронтенда](#43-запуск-фронтенда)
   - [4.4 Тестовые учётные записи](#44-тестовые-учётные-записи)
5. [Структура проекта и описание файлов](#-структура-проекта-и-описание-файлов)
   - [5.1 Корневая структура](#51-корневая-структура)
   - [5.2 Бэкенд (backend/)](#52-бэкенд-backend)
   - [5.3 Фронтенд (frontend/)](#53-фронтенд-frontend)
6. [Все использованные команды](#-все-использованные-команды-npm--npx--git)
   - [6.1 Команды для бэкенда](#61-команды-для-бэкенда)
   - [6.2 Команды для фронтенда](#62-команды-для-фронтенда)
   - [6.3 Команды для Git и деплоя](#63-команды-для-git-и-деплоя)
7. [Возможности сайта и управление](#-возможности-сайта-и-управление)
   - [7.1 Для гостей (неавторизованных)](#71-для-гостей-неавторизованных)
   - [7.2 Для жителей (авторизованных)](#72-для-жителей-авторизованных)
   - [7.3 Для председателя](#73-для-председателя)
   - [7.4 Для администратора](#74-для-администратора)
   - [7.5 Переключение между посёлками](#75-переключение-между-посёлками)
   - [7.6 Адаптивный дизайн](#76-адаптивный-дизайн)
   - [7.7 SEO-оптимизация](#77-seo-оптимизация)
8. [API (бэкенд) – полная спецификация](#-api-бэкенд--полная-спецификация)
   - [8.1 Публичные эндпоинты](#81-публичные-эндпоинты)
   - [8.2 Защищённые эндпоинты (требуют JWT)](#82-защищённые-эндпоинты-требуют-jwt)
   - [8.3 Административные эндпоинты](#83-административные-эндпоинты)
9. [Сокращения и аббревиатуры](#-сокращения-и-аббревиатуры)
10. [Устранение неполадок (FAQ)](#-устранение-неполадок-faq)
11. [Деплой на хостинг](#-деплой-на-хостинг)
    - [11.1 Деплой бэкенда на Render.com](#111-деплой-бэкенда-на-rendercom)
    - [11.2 Деплой фронтенда на Netlify](#112-деплой-фронтенда-на-netlify)
    - [11.3 Настройка домена и SSL](#113-настройка-домена-и-ssl)
12. [Лицензия](#-лицензия)

---

## 🌟 О проекте

**Единый информационный веб-ресурс** для двух дачных посёлков — **ДПК «Заповедное»** и **ДПК «Колосок»**.

**Ключевые особенности:**
- Два логических подсайта: `/zapovednoe` и `/kolosok`.
- Автоматический редирект с `/` на `/zapovednoe`.
- Мгновенное переключение между посёлками (без перезагрузки страницы).
- Разделы: Главная, О посёлке, Участки, Инфраструктура, Галерея, Документы, Контакты, Новости.
- Полноценная **админ-панель (CMS)** с разграничением прав:
  - **Администратор** — полный контроль над всем.
  - **Председатель** — управление только своим посёлком.
  - **Житель** — после входа видит документы своего посёлка.
  - **Гость** — просмотр общедоступного контента.
- Адаптивный дизайн (ПК, планшеты, смартфоны).
- SEO-готовность (мета-теги, robots.txt, sitemap.xml).

---

## 🛠️ Технологический стек

| Компонент       | Технологии                                                                 |
|----------------|----------------------------------------------------------------------------|
| **Фронтенд**   | React 18, React Router DOM 6, Axios, React Helmet Async                    |
| **Бэкенд**     | Node.js, Express 4, SQLite3, bcrypt, JSON Web Token (JWT), Multer, CORS    |
| **Стили**      | Чистый CSS (адаптивная вёрстка, две цветовые темы для посёлков)            |
| **База данных**| SQLite (файл `database.sqlite`) – не требует отдельного сервера БД         |
| **Загрузка файлов** | Multer (сохранение в папку `uploads/`)                               |
| **Разработка** | Nodemon (авто-перезагрузка бэкенда), Create React App                      |

---

## 💻 Требования для запуска

- **Node.js** версии 14 или выше (скачать с [nodejs.org](https://nodejs.org/))
- **npm** (устанавливается вместе с Node.js, проверьте командой `npm --version`)
- **Git** (опционально, для клонирования репозитория)
- **Браузер** (Chrome, Firefox, Safari, Edge – последние версии)
- **Свободные порты:** 5000 (бэкенд) и 3000 (фронтенд)

---

## 🚀 Установка и запуск (локально)

### 4.1 Клонирование репозитория

```bash
git clone https://github.com/ваш-логин/dpk-site.git
cd dpk-site
```

Если у вас нет Git, скачайте ZIP-архив с GitHub и распакуйте.

### 4.2 Запуск бэкенда

Откройте **первый терминал** (или командную строку) и выполните:

```bash
cd backend
npm install
npm run dev
```

**Что произойдёт:**
- Установятся все зависимости (Express, SQLite и др.)
- Создастся файл `database.sqlite` в папке `backend`.
- Создадутся таблицы: `users`, `news`, `documents`, `plots`, `infrastructure`, `gallery`, `contacts`.
- Добавятся три тестовых пользователя (см. раздел 4.4).
- Сервер начнёт слушать порт 5000.

Вы увидите сообщение:
```
Сервер запущен на http://localhost:5000
```

**Если возникла ошибка `SQLITE_ERROR: no such table`** – удалите `database.sqlite` и перезапустите.

### 4.3 Запуск фронтенда

Откройте **второй терминал** (не закрывая первый) и выполните:

```bash
cd frontend
npm install
npm install react-helmet-async   # если пакет не был установлен автоматически
npm start
```

- Автоматически откроется браузер с адресом `http://localhost:3000`.
- Вы будете перенаправлены на `http://localhost:3000/zapovednoe`.

### 4.4 Тестовые учётные записи

| Роль           | Email                          | Пароль      |
|----------------|--------------------------------|-------------|
| **Администратор**  | `admin@dpk.ru`                 | `admin123`  |
| **Председатель**   | `chair@zapovednoe.ru`          | `chair123`  |
| **Житель**         | `resident@kolosok.ru`          | `resident123`|

> Используйте их для входа на сайт (кнопка «Вход» в шапке).

---

## 📂 Структура проекта и описание файлов

### 5.1 Корневая структура

```
dpk-site/
├── backend/                # Серверная часть (Node.js + Express)
├── frontend/               # Клиентская часть (React)
├── .gitignore              # Файлы и папки, исключённые из Git
└── README.md               # Этот документ
```

### 5.2 Бэкенд (`backend/`)

| Файл / папка               | Подробное описание |
|----------------------------|---------------------|
| `server.js`                | **Главный файл сервера.** Настраивает Express, подключает middleware (CORS, helmet, morgan, JSON), монтирует все маршруты (`/api/auth`, `/api/news` и т.д.), запускает слушатель порта. |
| `database.js`              | **Инициализация SQLite.** Создаёт все таблицы, если их нет. Добавляет тестовых пользователей при первом запуске. Экспортирует объект `db` для запросов. |
| `models/`                  | **Модели данных** – каждая модель отвечает за CRUD-операции с одной таблицей. |
| `models/User.js`           | Методы: `findByEmail`, `create`, `findById`, `updateRole`, `getAll`. |
| `models/News.js`           | `getAll`, `getById`, `create`, `update`, `delete`. |
| `models/Document.js`       | Аналогично для документов. |
| `models/Plot.js`           | Участки (с фильтрацией по площади, цене, статусу). |
| `models/Infrastructure.js` | Объекты инфраструктуры (дороги, электричество и т.п.). |
| `models/Gallery.js`        | Фото и видео для галереи. |
| `models/Contact.js`        | Сообщения из формы обратной связи. |
| `routes/`                  | **Маршруты API.** |
| `routes/auth.js`           | Регистрация, логин, получение текущего пользователя. |
| `routes/news.js`           | CRUD новостей (доступ по ролям). |
| `routes/documents.js`      | CRUD документов (доступ только авторизованным, житель – только свой). |
| `routes/plots.js`          | CRUD участков (фильтры передаются через query-параметры). |
| `routes/infrastructure.js` | CRUD объектов инфраструктуры. |
| `routes/gallery.js`        | Добавление / удаление фото/видео. |
| `routes/upload.js`         | Загрузка файлов через Multer (сохранение в `uploads/`). |
| `routes/contacts.js`       | Отправка и получение сообщений. |
| `routes/settlements.js`    | Статические данные о посёлках (адрес, телефон, карта). |
| `routes/admin.js`          | Управление пользователями (получение списка, изменение роли) с учётом прав admin/chairman. |
| `middleware/`              | **Промежуточные обработчики.** |
| `middleware/auth.js`       | Проверяет JWT-токен из заголовка `Authorization`. Если токен валиден, добавляет `req.user`. |
| `middleware/roleCheck.js`  | Принимает список разрешённых ролей и проверяет, что роль пользователя входит в этот список. |
| `uploads/`                 | **Папка для загруженных файлов.** Создаётся автоматически при первом запросе загрузки. |
| `database.sqlite`          | **Файл базы данных SQLite.** Создаётся при первом запуске. |
| `.env`                     | **Переменные окружения** (порт, JWT_SECRET). Не публикуется в Git. |
| `package.json`             | Список зависимостей и скрипты запуска (`npm run dev`, `npm start`). |

### 5.3 Фронтенд (`frontend/`)

| Файл / папка                     | Подробное описание |
|----------------------------------|---------------------|
| `public/`                        | Статические файлы, которые копируются в сборку. |
| `public/index.html`              | HTML-шаблон (стандартный Create React App). |
| `public/robots.txt`              | Инструкции для поисковых роботов (разрешить индексацию, указать sitemap). |
| `public/sitemap.xml`             | Карта сайта (список всех URL для SEO). |
| `src/index.js`                   | Точка входа React. Оборачивает приложение в `HelmetProvider` (для мета-тегов) и рендерит `App`. |
| `src/App.js`                     | Главный компонент с маршрутизацией (`react-router-dom`). Определяет layout для посёлков, защищённые маршруты (`PrivateRoute`), админ-маршруты. |
| `src/App.css`                    | Глобальные стили: сетка, карточки, формы, кнопки. Дополнительно две темы (`.theme-zapovednoe` – зелёная, `.theme-kolosok` – синяя). |
| `src/context/AuthContext.js`     | Контекст авторизации. Хранит состояние пользователя, токен. Предоставляет методы `login`, `register`, `logout`, а также флаги `isAdmin`, `isChairman`, `isResident`, `canEditSettlement`. |
| `src/services/`                  | **API-клиенты** – каждый файл экспортирует функции для запросов к конкретному эндпоинту. |
| `src/services/api.js`            | Базовый экземпляр axios с `baseURL = 'http://localhost:5000/api'`. |
| `src/services/auth.js`           | `login`, `register`, `getCurrentUser`. |
| `src/services/news.js`           | `getNews`, `createNews`, `updateNews`, `deleteNews`. |
| `src/services/documents.js`      | Аналогично. |
| `src/services/plots.js`          | `getPlots`, `createPlot`, `updatePlot`, `deletePlot`. |
| `src/services/infrastructure.js` | CRUD инфраструктуры. |
| `src/services/gallery.js`        | `getGallery`, `createGalleryItem`, `deleteGalleryItem`. |
| `src/services/upload.js`         | `uploadFile` (FormData). |
| `src/services/contacts.js`       | `sendContactMessage`, `getContactMessages`. |
| `src/services/settlements.js`    | `getSettlementInfo`. |
| `src/components/`                | **Переиспользуемые компоненты.** |
| `src/components/Header.js`       | Шапка сайта: логотип, навигация, кнопка выхода, переключатель посёлков. Динамическая тема (цвет меняется в зависимости от settlement). |
| `src/components/Footer.js`       | Футер с копирайтом. |
| `src/components/SettlementSwitcher.js` | Две кнопки для переключения между «Заповедное» и «Колосок». Работает через React Router. |
| `src/components/PrivateRoute.js` | Если пользователь не авторизован – редирект на `/login`. |
| `src/components/AdminRoute.js`   | Доступ только для администратора (иначе редирект на `/`). |
| `src/components/ChairmanRoute.js`| Доступ для администратора или председателя. |
| `src/pages/`                     | **Страницы приложения.** |
| `src/pages/MainPage.js`          | Корневой маршрут `/` – сразу редиректит на `/zapovednoe`. |
| `src/pages/SettlementLayout.js`  | Layout для подсайта: содержит `Header`, `Outlet` (вложенные страницы), `Footer`. Извлекает `settlement` из URL и передаёт через контекст. |
| `src/pages/Home.js`              | Главная страница: приветствие, последние новости, мини-галерея. Мета-теги. |
| `src/pages/About.js`             | Информация о посёлке (адрес, телефон, описание, устав). |
| `src/pages/Plots.js`             | Каталог участков с фильтрами (площадь, цена, статус) и картой (iframe). |
| `src/pages/Infrastructure.js`    | Список объектов инфраструктуры (иконка, название, описание). |
| `src/pages/Gallery.js`           | Сетка фото и видео. |
| `src/pages/Documents.js`         | Доступен только авторизованным. Житель видит документы только своего посёлка. Мета-теги с `noindex`. |
| `src/pages/Contacts.js`          | Контактная информация, карта проезда, форма обратной связи (отправка на бэкенд). |
| `src/pages/News.js`              | Два компонента: `NewsList` (список новостей) и `NewsDetail` (полный текст). |
| `src/pages/Login.js`             | Страница входа. Мета-теги `noindex`. |
| `src/pages/Register.js`          | Регистрация нового жителя. |
| `src/pages/admin/`               | **Админ-панель.** |
| `src/pages/admin/AdminPanel.js`  | Главная админки: кнопки-ссылки на разделы, приветствие. |
| `src/pages/admin/AdminNews.js`   | Управление новостями (список, добавление, редактирование, удаление, загрузка изображений). |
| `src/pages/admin/AdminDocuments.js` | Управление документами (загрузка PDF и других файлов). |
| `src/pages/admin/AdminPlots.js`  | CRUD участков. |
| `src/pages/admin/AdminInfrastructure.js` | CRUD объектов инфраструктуры. |
| `src/pages/admin/AdminGallery.js`| Добавление фото/видео, удаление. |
| `src/pages/admin/AdminUsers.js`  | Управление пользователями (изменение ролей). Админ видит всех, председатель – только жителей своего посёлка. |
| `src/utils/`                     | Вспомогательные утилиты. |
| `src/utils/seo.js`               | Функция `getSEOTags(settlement, pageTitle, pageDescription, keywords)` – генерирует объект с title и description. |
| `src/utils/constants.js`         | Константы `SETTLEMENTS`, `SETTLEMENTS_NAMES`. |

---

## 📦 Все использованные команды (npm, npx, git)

### 6.1 Команды для бэкенда

```bash
# Переход в папку backend
cd backend

# Инициализация package.json
npm init -y

# Установка основных зависимостей
npm install express sqlite3 sqlite bcrypt jsonwebtoken dotenv multer cors helmet morgan express-validator

# Установка dev-зависимости nodemon для авто-перезагрузки
npm install -D nodemon

# Запуск в режиме разработки (с nodemon)
npm run dev

# Запуск в обычном режиме
npm start
```

**Содержимое `backend/package.json` (скрипты):**
```json
"scripts": {
  "start": "node server.js",
  "dev": "nodemon server.js"
}
```

### 6.2 Команды для фронтенда

```bash
# Создание React-приложения (если создаёте с нуля)
npx create-react-app frontend
cd frontend

# Установка зависимостей
npm install axios react-router-dom
npm install react-helmet-async

# Запуск в режиме разработки
npm start

# Сборка для продакшена
npm run build
```

### 6.3 Команды для Git и деплоя

```bash
# Инициализация репозитория (в корне проекта)
git init

# Добавление всех файлов (кроме указанных в .gitignore)
git add .

# Первый коммит
git commit -m "Initial commit: full project"

# Привязка к удалённому репозиторию GitHub
git remote add origin https://github.com/ваш-логин/dpk-site.git

# Переименование ветки в main (если нужно)
git branch -M main

# Отправка на GitHub
git push -u origin main
```

---

## 🧠 Возможности сайта и управление

### 7.1 Для гостей (неавторизованных)

- **Главная** – краткое описание посёлка, 3 последние новости, 4 последних фото.
- **О посёлке** – информация из бэкенда (адрес, телефон, описание, устав).
- **Участки** – каталог с фильтрацией по площади, цене, статусу. Внизу – карта (iframe Яндекс.Карт).
- **Инфраструктура** – карточки с иконками (например, ⚡ электричество, 💧 вода).
- **Галерея** – сетка фото и видео.
- **Новости** – список всех новостей, клик по заголовку открывает детальную страницу.
- **Контакты** – адрес, карта проезда, форма обратной связи (отправляет сообщение в БД).

### 7.2 Для жителей (авторизованных)

- **Документы** – раздел становится доступным. Житель видит только документы, загруженные для **его** посёлка.
- Всё остальное – как у гостя.

### 7.3 Для председателя

- Доступ к админ-панели через кнопку «Админка» в шапке.
- Возможность **добавлять, редактировать, удалять**:
  - Новости (только своего посёлка)
  - Документы (только своего)
  - Участки (только своего)
  - Объекты инфраструктуры (только своего)
  - Фото и видео в галерею (только своего)
- Просмотр сообщений из формы обратной связи (только для своего посёлка).
- Управление пользователями своего посёлка: может изменить роль **жителя** на **гостя** и наоборот.

### 7.4 Для администратора

- Полный доступ ко **всем данным** обоих посёлков.
- Может назначать любые роли (`admin`, `chairman`, `resident`, `guest`) любым пользователям.
- Видит все сообщения из формы обратной связи (всех посёлков).

### 7.5 Переключение между посёлками

- В правой части шапки – две кнопки: **«Заповедное»** и **«Колосок»**.
- При клике меняется URL и весь контент (главная, участки, документы и т.д.) обновляется без перезагрузки страницы.
- Цветовая тема также меняется (зелёная для Заповедного, синяя для Колоска).

### 7.6 Адаптивный дизайн

- На узких экранах (мобильные устройства) шапка сворачивается, меню становится вертикальным или переносится.
- Карточки участков и галереи строятся в одну колонку.
- Формы и таблицы горизонтально прокручиваются.

### 7.7 SEO-оптимизация

- **Динамические мета-теги** через `react-helmet-async`:
  - `title` – уникальный для каждой страницы (например, «Участки | ДПК Заповедное»)
  - `description` – краткое описание содержания
  - `keywords` – ключевые слова
- Статические файлы:
  - `robots.txt` – разрешает индексацию всего сайта, указывает путь к sitemap
  - `sitemap.xml` – содержит список всех страниц (как для /zapovednoe, так и для /kolosok)
- Для страниц «Документы», «Вход», «Регистрация» добавлен мета-тег `<meta name="robots" content="noindex, follow">`, чтобы поисковики не индексировали их.

---

## 🔌 API (бэкенд) – полная спецификация

**Базовый URL:** `http://localhost:5000/api` (при локальном запуске).

### 8.1 Публичные эндпоинты

| Метод | URL | Описание | Пример тела запроса |
|-------|-----|----------|----------------------|
| POST | `/auth/register` | Регистрация жителя | `{"email":"a@a.ru","password":"123456","fullName":"Иван","settlement":"zapovednoe"}` |
| POST | `/auth/login` | Вход, возвращает JWT | `{"email":"admin@dpk.ru","password":"admin123"}` |
| GET | `/news?settlement=zapovednoe` | Список новостей | – |
| GET | `/news/:id` | Одна новость | – |
| GET | `/plots?settlement=kolosok&minArea=5` | Участки (фильтры опциональны) | – |
| GET | `/plots/:id` | Один участок | – |
| GET | `/infrastructure?settlement=zapovednoe` | Инфраструктура | – |
| GET | `/gallery?settlement=kolosok` | Галерея | – |
| POST | `/contacts` | Отправить сообщение | `{"name":"Иван","email":"i@i.ru","message":"Привет","settlement":"zapovednoe"}` |
| GET | `/settlements/zapovednoe` | Статическая информация о посёлке | – |

### 8.2 Защищённые эндпоинты (требуют JWT)

> **Заголовок:** `Authorization: Bearer <token>`

| Метод | URL | Роли | Описание |
|-------|-----|------|----------|
| GET | `/auth/me` | Любая авторизованная | Получить данные текущего пользователя |
| GET | `/documents?settlement=zapovednoe` | Авторизованные (житель – только свой) | Список документов |
| POST | `/news` | admin, chairman | Создать новость |
| PUT | `/news/:id` | admin, chairman | Обновить новость |
| DELETE | `/news/:id` | admin, chairman | Удалить новость |
| POST | `/documents` | admin, chairman | Добавить документ |
| PUT | `/documents/:id` | admin, chairman | Обновить документ |
| DELETE | `/documents/:id` | admin, chairman | Удалить документ |
| POST | `/plots` | admin, chairman | Добавить участок |
| PUT | `/plots/:id` | admin, chairman | Обновить участок |
| DELETE | `/plots/:id` | admin, chairman | Удалить участок |
| POST | `/infrastructure` | admin, chairman | Добавить объект инфраструктуры |
| PUT | `/infrastructure/:id` | admin, chairman | Обновить |
| DELETE | `/infrastructure/:id` | admin, chairman | Удалить |
| POST | `/gallery` | admin, chairman | Добавить фото/видео |
| DELETE | `/gallery/:id` | admin, chairman | Удалить |
| POST | `/upload` | admin, chairman | Загрузить файл (мультипарт) |
| GET | `/contacts` | admin, chairman | Просмотр сообщений (председатель – только своего) |

### 8.3 Административные эндпоинты

| Метод | URL | Роли | Описание |
|-------|-----|------|----------|
| GET | `/admin/users` | admin (все), chairman (только своего) | Список пользователей |
| PUT | `/admin/users/:id/role` | admin (любая роль), chairman (resident/guest) | Изменить роль пользователя |

---

## 📖 Сокращения и аббревиатуры

| Сокращение | Полное название (рус.) | Пояснение |
|------------|------------------------|------------|
| **ДПК** | Дачное некоммерческое партнёрство | Организация, управляющая общим имуществом в дачном посёлке. |
| **ДНП** | Дачное некоммерческое партнёрство | Аналог ДПК (термин часто используется взаимозаменяемо). |
| **CMS** | Content Management System | Система управления контентом (админ-панель). |
| **SPA** | Single Page Application | Одностраничное приложение; страницы переключаются без полной перезагрузки. |
| **API** | Application Programming Interface | Набор правил и эндпоинтов для обмена данными между клиентом и сервером. |
| **REST** | Representational State Transfer | Архитектурный стиль, при котором ресурсы имеют свои URL и методы HTTP. |
| **CRUD** | Create, Read, Update, Delete | Базовые операции с данными (создание, чтение, обновление, удаление). |
| **JWT** | JSON Web Token | Компактный токен, содержащий информацию о пользователе (id, роль). Подписывается сервером. |
| **SQLite** | SQLite | Встраиваемая реляционная БД, хранится в одном файле (`database.sqlite`). |
| **БД** | База данных | Хранилище структурированной информации. |
| **SSL** | Secure Sockets Layer | Протокол шифрования для HTTPS. |
| **CORS** | Cross-Origin Resource Sharing | Механизм, позволяющий веб-странице запрашивать ресурсы с другого домена/порта. |
| **.env** | Environment file | Файл с переменными окружения (пароли, порты, секреты). Не публикуется в Git. |
| **SEO** | Search Engine Optimization | Комплекс мер для улучшения видимости сайта в поисковых системах. |
| **XML** | eXtensible Markup Language | Формат для представления данных (используется в sitemap.xml). |
| **HTTP** | HyperText Transfer Protocol | Протокол передачи гипертекста. |
| **HTTPS** | HTTP Secure | Защищённая версия HTTP с шифрованием. |
| **JSON** | JavaScript Object Notation | Лёгкий формат обмена данными, похожий на объекты JavaScript. |
| **Middleware** | Промежуточное ПО | Функции в Express, которые обрабатывают запрос до основного обработчика. |
| **Multer** | Multer | Middleware для обработки `multipart/form-data` (загрузка файлов). |
| **bcrypt** | bcrypt | Библиотека для хеширования паролей. |
| **Axios** | Axios | HTTP-клиент для браузера и Node.js. |

---

## 🩺 Устранение неполадок (FAQ)

| Проблема | Вероятная причина | Решение |
|----------|-------------------|---------|
| **Ошибка `SQLITE_ERROR: no such table`** | База данных не создалась или таблицы отсутствуют. | Удалите `backend/database.sqlite` и перезапустите бэкенд. |
| **Ошибка CORS в браузере** | Фронтенд (порт 3000) пытается обратиться к бэкенду (порт 5000), но CORS не разрешает. | Убедитесь, что в `server.js` подключён `cors()`. Также проверьте, что бэкенд запущен. |
| **Файлы не загружаются (uploads)** | Отсутствует папка `uploads/` или нет прав на запись. | Создайте папку `backend/uploads/` вручную и дайте права `chmod 755`. |
| **React не запускается: `Module not found: react-helmet-async`** | Пакет не установлен. | Выполните `npm install react-helmet-async` в папке `frontend`. |
| **При переключении посёлка тема не меняется** | В `Header.js` не используется `themeClass`. | Проверьте, что компонент Header получает `settlement` и добавляет класс `<header className={themeClass}>`. |
| **Документы недоступны даже после входа** | Возможно, у пользователя роль `guest` или он не из того посёлка. | Зайдите в админ-панель и проверьте/измените роль. |
| **Админ-панель пустая или нет кнопок** | Не установлены компоненты админки или не настроены маршруты. | Убедитесь, что в `App.js` есть `<Route path="/admin" element={...}>` и все дочерние маршруты. |
| **При отправке формы обратной связи ошибка 500** | Бэкенд не может сохранить запись в таблицу `contacts`. | Проверьте, что таблица `contacts` создана (посмотрите `database.js`). Добавьте её, если отсутствует. |
| **Карта участков не отображается** | Используется iframe с заглушкой. | Получите API-ключ Яндекс.Карт и замените src на реальный конструктор карты с отметками участков. |
| **Не удаётся войти с тестовыми учётными данными** | Пользователи не были созданы. | При первом запуске бэкенда они добавляются автоматически. Если нет – вставьте SQL-запрос вручную через `sqlite3`. |

---

## 🌍 Деплой на хостинг

### 11.1 Деплой бэкенда на Render.com

1. Зарегистрируйтесь на [render.com](https://render.com).
2. Нажмите **New +** → **Web Service**.
3. Подключите ваш GitHub-репозиторий.
4. Настройте:
   - **Name**: `dpk-backend`
   - **Root Directory**: `backend`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
5. В разделе **Environment Variables** добавьте:
   - `JWT_SECRET` = любая длинная строка
   - `PORT` = `5000` (или оставить пустым, Render задаст свой)
6. Нажмите **Create Web Service**.
7. После деплоя вы получите URL вида `https://dpk-backend.onrender.com`.

### 11.2 Деплой фронтенда на Netlify

1. Зарегистрируйтесь на [netlify.com](https://netlify.com).
2. Нажмите **Add new site** → **Import an existing project**.
3. Подключите GitHub и выберите репозиторий.
4. Настройте:
   - **Base directory**: `frontend`
   - **Build command**: `npm run build`
   - **Publish directory**: `frontend/build`
5. В разделе **Environment variables** создайте переменную:
   - `REACT_APP_API_URL` = URL вашего бэкенда на Render (например, `https://dpk-backend.onrender.com/api`)
   - **Важно:** после этого в `frontend/src/services/api.js` нужно использовать `process.env.REACT_APP_API_URL`.
6. Нажмите **Deploy site**.

### 11.3 Настройка домена и SSL

- На Render: перейдите в настройки вашего Web Service → **Custom Domain** → добавьте домен (например, `api.dpk-site.ru`). SSL выдаётся автоматически.
- На Netlify: **Domain settings** → **Add custom domain** → укажите ваш домен (например, `dpk-site.ru`). Netlify также автоматически предоставляет SSL-сертификат через Let's Encrypt.

**Пример файла `api.js` для продакшена:**
```javascript
const api = axios.create({
  baseURL: process.env.REACT_APP_API_URL || 'http://localhost:5000/api',
});
```

---

## 📄 Лицензия

Проект разработан в рамках учебной производственной практики для студентов III курса по специальности **09.02.07 Информационные системы и программирование**.

Исходный код может быть использован в образовательных целях. Коммерческое распространение запрещено без согласия авторов.

---

## 👥 Контакты

По вопросам установки, настройки и доработок обращайтесь к разработчику:

- **Email:** Myakolka@mail.ru
- **Номер телефона:** +79265671347

---

*Документ составлен в мае 2026 года. Версия 2.0 (полное руководство).*
```
